# Main window

Layout geometry, the Liquid Glass chrome, the art-tint/accent color pipeline and the codec line's rendering live in **`APPEARANCE.md`, in this directory — read it before changing any of those.** This file covers behavior: the controller, transport, the convert swap and its undo, display states, and the window's resize and drop rules.

## Bootstrap

`MainWindow` configures itself in `init`: borderless, frame autosave, drag-and-drop registration. `MainPlayerContentView`, a plain transparent `NSView`, holds the hierarchy and exposes subviews as readonly properties; the glass backdrop and pitch panel are `contentView` siblings installed by the controller. `MainPlayerController` adopts the hierarchy as outlets in `buildContentInWindow:` (called from `init`) and invokes `windowDidLoad` by hand — AppKit only fires it on the nib path. Playlist cells are built in `PlaylistTableView`'s `makeCellViewWithIdentifier:` and recycled through the table's normal reuse queue.

## The controller

`MainPlayerController` (`NSWindowController`) is the central coordinator. Its public header exposes only collaborators, actions and `NSMenuDelegate` (public so `MainMenuBuilder` can wire the waveform-style submenu's delegate). What is left in `MainPlayerController.m` itself is the coordination: the `updateUI` funnel, the display-state resolution, the open and playlist entry points, and the settings live-apply hooks. Everything else is a category:

- `+Window` — building the content into the window, the two sibling frames, the resize, occlusion and restoration rules, and the actions that change the window's shape or appearance (Size presets, pitch-panel reveal, always-on-top, light/dark). `NSWindowDelegate` and `NSWindowRestoration` declared on the category.
- `+PlayerEvents` — every `AudioPlayerDelegate` callback. Two rules govern the whole file and are stated at its top: **every callback can be stale** and must match the delivered track against the playlist's current one, and **`stop` fires no callback**, so nothing there drives auto-advance.
- `+Delivery` — where asynchronous results land: metadata, waveform snapshots, detected BPM and key, plus the waveform view's scrub seek. All of them implement the one cross-directory invariant about deliveries racing track changes, which is why they are one file.
- `+Menus` — menu validation and the waveform-style submenu; `NSMenuItemValidation` conformance declared on the category.
- `+NowPlaying` — the `updateNowPlaying` publish and `NowPlayingControllerDelegate` routing; same conformance-on-category pattern.
- `+Transport` — relative-seek skips and DJ effect toggles; declared in the category header, touching only public collaborators.
- `+Convert` — the Convert to FLAC funnel, the playlist swap and the undo round trip, with `VibeFLACConversionRecord` riding along; the actions are declared in the category header, the Transport pattern. The engine is `Audio/Convert/`.
- `+Debug.h` — the debug command channel's extra surface, in `Vibe/Debug/Introspection/`.

**`MainPlayerControllerInternal.h` is the one private surface all of them share** — the class extension with the outlets, the collaborator handles, the ivars a category touches, and the internal methods. It replaced four hand-rolled "support" interfaces the categories each carried, which were four partial, overlapping views of one surface and a *runtime crash* rather than a compile error whenever one drifted. A method a category implements is declared in **that category's header**, never in `MainPlayerController.h` or the internal header, or the compiler cannot check either side.

**The controller owns folder art's invalidation and its redraws**, though the feature itself lives in `Audio/Metadata/FolderArtResolver` (see `Audio/Metadata/CLAUDE.md` for the cost rules). `refreshFolderArt` — public — runs when Settings > Files changes the album-art setting, and calls the resolver's `folderArtSettingDidChange`: the setting is *cached* there, on the hot cell-draw path, so this call is what makes the write observable at all, not merely a redraw. It deliberately keeps the settled answers, since the setting decides whether the fallback is consulted and never what a folder holds. A **grant** change is answered separately in `grantedFoldersDidChange:`, with the resolver's narrow `invalidateDirectoriesSettledWithoutGrant`: a grant unlocks folders the resolver declined to touch and cannot change a cover already found. **Both were once a full wipe and both times the wipe was the bug** — it threw away the covers a folder walk had harvested for free and left those folders to the lone-file stat probes, which know three of the candidate names (see `Audio/Metadata/CLAUDE.md`). The controller observes `FolderAccessManagerDidChangeNotification` itself, because a grant usually arrives from a drop or an open with that pane never opened. `folderArtDidResolve:` observes the resolver's own notification — which fires for "this folder has none" as well as for a cover, since the header holds the previous track's art until it hears — and coalesces the redraw over a short delay, reloading the *visible* rows alone; the delay is the point, since the resolver is serial and a per-run-loop-turn gate would coalesce nothing. These two observers are why the class has a `dealloc`.

`TransportKeyMonitor` handles the bare keys: Space, B, N, P, Tab; A/S/D skip forward, Z/X/C skip back; and the dual-mode effect keys Q, W, E, R, T (R = 1/8-note delay taps, T = 1/16). A tap toggles, a hold is momentary: the effect flips at keyDown, and keyUp reverts to the pre-press state when the press ran past the ~0.35 s tap threshold — keyUp is what decides tap vs hold.

The five effect-state setters in `+Transport` are the single funnel for menus, bare-key taps and holds, and debug commands; each calls `updateFXIndicators`, which re-reads the live `AudioFX` flags rather than trusting the caller's intent. The codec line doubles as the FX indicator; its rendering is in `APPEARANCE.md`.

A `UIUpdateTimer` (this directory — it serves exactly this one screen) drives `updatePlaybackUI`, only while playback wants updates and the window is unoccluded; `windowDidChangeOcclusionState:` pushes the gate and refreshes once on reveal, since Control Center keeps counting on its own. The full `updateUI` runs on transport events and metadata deliveries. Metadata loading for a dropped playlist waits until playback starts (two-second fallback), so the first track plays immediately on a large folder drop.

Its rate is **scaled to the playhead's on-screen speed** rather than fixed: `syncUITimerRate` feeds the waveform's `devicePixelWidth`, the cached duration and the varispeed rate to `VibeUIUpdateHzForPlayhead` (`MainWindow/UIUpdateMath.h`, header-only and tested), which asks for the Hz that steps the playhead ~2 device pixels, floored at 3 and capped by `AppSettings.uiUpdateHzCap` (Settings > Advanced, "Playhead refresh": 3, 30 by default, or 60 Hz — a cap of 3 is the fixed rate the app ticked at before this rule). An ordinary song computes under the floor and so costs exactly what it always did whatever the cap says; a five-second sample, whose playhead crosses ~240 px/s, takes the whole cap. The inputs move at a track start and any transport event (the `updateUI` funnel), a fader tick (`updateRateDependentUI`), a resize (`windowDidResize:`, live-drag frames included) and the cap itself (the Settings pane, through the public `syncUITimerRate`), and each of those calls the sync. The higher rate is affordable only because the expensive paths self-gate — `AudioWaveformView.setProgress:` repaints on device-pixel crossings, using that same `devicePixelWidth`, and the time labels are change-guarded at one second — so the extra ticks mostly return having drawn nothing. Read the chosen rate back with `dump_state`'s `ui.uiUpdateHz`; `check_invariants` pairs it with the rule re-applied to the live inputs (`ui.update_rate_follows_inputs`), which is what catches a path that moves one of them without resyncing.

## Skipping and closing

The skip actions seek by bars when the tempo is known — tagged BPM beats analyzed, same precedence as the BPM label. The three sizes are `AppSettings.skipBaseBars` (Settings > Playback: 4, 8 or 16, default 8), twice it and four times it. Bars are fixed spans of file time, so the jump stays on the musical grid at any pitch. Without a tempo: 10/30/60 seconds in the pitch-adjusted wall-clock seconds the labels show.

A forward skip past the end calls `AudioPlayer.finishCurrentTrack`, which fires `didFinishPlaying:`, so the usual auto-advance / end-of-playlist path handles it — no bespoke branch.

Skips need a player that is not Stopped, gated in both `skipByFileSeconds:` (bare keys bypass validation) and menu validation. After the playlist ends the finished file stays open, so `duration` alone still looks seekable while no node exists.

File > Close (⌘W, `closeFile:`, retitled "Close File" / "Close All Files" in validation, enabled only while the player window is key so ⌘W in Settings or About never unloads the playlist) calls `AudioPlayer.stop`, which fires no delegate event, then clears the playlist, cancels the deferred metadata load, drops the scan loader with `cancelAll` — a cancelled loader still strongly holds every queued track, thumbnails included — and returns to the empty state.

## Convert to FLAC: the swap

The conversion lives in `Audio/Convert/`, and the controller half below in `MainPlayerController+Convert`; this is what happens when it finishes: the FLAC takes the row its source occupied, so the action reads as the file changing format in place.

`didConvertTrack:toURL:error:` resolves the source's row *at completion time*, because a re-drop during the encode replaces the playlist — then there is nothing to swap and the FLAC stays on disk. Four outcomes: row gone → nothing; row current and loaded → replay; row current but stopped → `updateUI` (the header reads the row through `displayedTrack`); row elsewhere → re-arm the prefetch when it is `currentIndex + 1`, since the parked handle is path-keyed and still points at the source.

The replay uses `AudioPlayer.play:atPosition:startPaused:`. The playlist entry is swapped **first**, so `didStartPlaying:`'s identity guard passes and the whole per-track refresh — waveform, codec label, artwork, Now Playing, Open Recent, prefetch — comes free with the ordinary play path. `PlaylistController.replaceTrackAtIndex:withURL:` mints a *fresh* `AudioTrack`: `AudioTrack` memoizes its cache key with no invalidation, and a track still carrying the WAV's key would file the FLAC's waveform and metadata under the WAV's entries. Duration and detected BPM carry across by hand — same audio, and re-deriving would blank the row until the new decode lands.

Both Convert to FLAC items — the Convert menu's and the window body's context-menu item below the Copy pair — share the `menu_convert_to_flac` identifier and one validation branch, deferring to `AudioFileConverter.validateConvertMenuItem:forTrack:`: "Converting…" while busy, "FLAC Already Exists" rather than a silently numbered copy. Both come from `MainMenuBuilder`'s vended item constructors, so the identifier and SF Symbol live in one file.

Disposing of the source is the last step, and the ordering is the point: swap first, then `trashSourceIfEnabled:convertedTo:` — the swap is what stops the row and the player from pointing at the file about to be deleted. The source URL is read before the swap (afterwards the old `AudioTrack` is one the playlist has let go of), and the trash runs whether or not a row was found: the encode succeeded either way. The disposal's completion is where the conversion becomes undoable: the controller builds a `VibeFLACConversionRecord` — source URL, output URL, where the Trash put the source (nil when Delete Original was off) — and registers "Convert to FLAC" with the window's `NSUndoManager`.

Progress is the waveform's brush-through sweep (`Waveform/CLAUDE.md`): `progressHandler` forwards the fraction to `trackDisplay.setConvertSweepFraction:` only while the converting track is `displayedTrack`, so a background conversion draws nothing and a track change stops the sweep at the next report. `didConvertTrack:` resets the front.

## Undo: the conversion round trip

Edit > Undo/Redo are `undo:` / `redo:` on the controller, thin forwards to the window's `NSUndoManager`; the Menus category validates and retitles from the manager. One conversion undo/redo transaction may be in flight at a time: the controller disables both items and rejects every action entry point until the async restore/trash chain settles, so rapid commands cannot interleave file moves against the same mutable record. The inverse-registration below runs *before* that in-flight check, not after — `NSUndoManager` has already popped the action by then, so bailing out first would drop the conversion off both stacks and make it permanently un-undoable. `undoConversion:` restores the trashed original, returns its rows through the same `swapConvertedTrack:toURL:` — a playing FLAC replays as the original at the same playhead — then trashes the FLAC: restore first because the swap replays from the restored file, FLAC last for the same reason the conversion trashes last. `redoConversion:` mirrors it: the FLAC comes back **from the Trash**, never a re-encode, and the original is re-trashed only if the conversion had trashed it (`sourceWasTrashed` — a Delete Original toggle flipped in between changes nothing). The record mutates in place as moves land — a nil Trash URL means that file is at its original path — so either direction proceeds sensibly after the other half-failed. A playlist replaced since means no row to swap; the files still round-trip.

TRAP: **each direction registers its inverse synchronously inside the invocation**, before any file move runs: only while the manager `isUndoing` does a registration land on the redo stack (mirrored for redo), and the moves outlive the invocation — registering from a completion would push the inverse onto the undo stack as a fresh action. So an inverse exists even when the moves then fail, and both directions re-check reality — the row via `trackForURL:`, the files via the record — rather than trusting the moves landed. A failed restore logs, beeps and reveals the stranded Trash item; the action falls off the stack and Finder's Put Back is the recovery.

## Time labels and display states

The right time label shows total duration (the default) or minus-prefixed remaining time counting down on the position tick; clicking toggles the mode, persisted in `AppSettings.showRemainingTime`. Both are wall-clock — file time over the varispeed rate — like the elapsed label, and every write goes through `renderRightTimeLabelWithDisplayPosition:duration:rate:` so the modes cannot drift. The tick skips the label when the duration cache is 0, or the end-of-playlist park's full-length resting value would be clobbered with `-0:00`.

Empty state: static midline waveform placeholder, both time labels `--:--`, a "Drop a file or press ⌘O" hint, all at half strength. At launch the `revealEmptyState` grace suppresses it to a blank header, so a launch-time open never flashes it.

Play errors render inline in the same style — no modal, no auto-skip. `displayedTrack` masks the errored track through the `_erroredTrack` / `_errorStatus` pair, written only via `setErrorMaskForTrack:status:` and `clearErrorMask`; the error text sits on the artist line over the title. The track stays in the playlist for a retry, and late metadata and art deliveries are ignored while masked.

All header rendering resolves through one five-state `TrackDisplayState` — track, loading, empty, launch-grace, error — resolved in one place (`displayState`) and shared by `updateUI`, `updatePlaybackUI` and the Now Playing publish, so the branches cannot drift. `TrackDisplayController` draws it: the header labels (title shrinks to fit), the codec/BPM corner, the drop hint, and the waveform's progress, shimmer and placeholder states. Pure decide-vs-draw: the display controller holds no player or playlist state.

## Now Playing

`NowPlayingController` (in this directory: the main window is its only driver) bridges to the system Now Playing UI (Control Center, media keys, AirPods/Bluetooth transport) via `MPRemoteCommandCenter` and `MPNowPlayingInfoCenter`; the republish position rule is header-only in `NowPlayingRules.h`, tested. The controller calls `updateNowPlaying` from the `updateUI` funnel plus the seek and pitch-change paths, publishing title, artist, artwork, duration, elapsed, rate and state; command handlers marshal delegate delivery to the main queue before routing to the same `playPause:`, `next:`, `previous:` and seek entry points the buttons use.

Position and duration are wall-clock, pitch-adjusted (file time ÷ varispeed `playbackRate`), matching the app's own labels, so Control Center tracks the pitch fader; the MediaPlayer rate is 1.0 while playing (wall-clock elapsed advances at real time), 0 paused. Nothing is published until the first track plays, so Vibe does not steal Now Playing at launch. Artwork reads non-blocking: `albumArt` returns only already-decoded art, falling back to the 128px `cachedThumbnail`, which the metadata worker decodes before the track publishes — a background round trip ahead of the full art, so the card fills in with the title instead of showing the app icon — and it refreshes when the full art resolves. It is scaled to 512px once, on the main thread, when the wrapper is built, rather than shipping the 1024px original to the media daemon on every publish. TRAP: **the `MPMediaItemArtwork` request handler must not draw.** It is invoked on the media daemon's threads, and the artwork it would draw from is the same live `NSImage` the header, dock tile and playlist cells are drawing — `NSImage` is not safe to draw concurrently — so the handler returns the pre-scaled image and lets MediaPlayer resize it. Inspect with `Vibe --debug-cmd dump_now_playing` — but **only from a launch without `--no-audio-hw`**, which suppresses the publish and the command registration outright (becoming the active media app takes auto-switching AirPods on its own, defeating that flag); under it the dump always reports `hasInfo: 0`. Use `VIBE_AUDIBLE=1` with the vibe-debug skill's `launch.sh`.

## The window

`MainWindow` accepts file drag-and-drop (`NSDraggingDestination`) and supports the small-large toggle. Freely resizable in both axes: the frame belongs to the user and autosave keeps it. The window enforces only the floor — `kMainWindowMinContentWidth` plus the pitch panel's slice when showing, and `kMainWindowSmallHeight` — and applies the app's own size changes: the playlist toggle's height, `kPitchPanelWidth` either way, the View > Size presets.

The height has a second floor that is a *band*: a playlist pane shorter than `kPlaylistPaneMinHeight` is a sliver — worst in the empty state, where the drop well runs out of room for its two lines, then hides at `PlaylistDropZoneView`'s visibility floor, leaving a blank strip. Nothing rests between `kMainWindowSmallHeight` and `kMainWindowMinLargeHeight`: `restingHeightForDraggedHeight:` sends a drag through the band to the nearer end (the playlist snaps shut and springs back open), and `loadSettings` clamps a restored frame out of it. `minSize` still floors at the collapsed height — the toggle and restore target it exactly. The rule reaches drags through `windowWillResize:toSize:` on the controller, gated on `inLiveResize` so the app's own animated resizes are not snapped mid-flight; every height the app sets is a fixed point of the rule anyway.

All three programmatic resizes animate at one fixed `animationResizeTime:` (`kWindowResizeAnimationDuration`) rather than AppKit's distance-scaled default, which drags on big jumps.

The pitch-panel toggle is the one place the two `contentView` siblings must not follow the resize — the reveal is the window's right edge sweeping past a stationary panel. `togglePitchPanel:` swaps in fixed masks for the animation and restores the resizable ones (body width-sizable, panel right-anchored) afterwards.

TRAP: **a Finder drag delivers file-reference URLs** (`file:///.file/id=…`), whose `.path` re-resolves to wherever the file currently *is*, while everything downstream treats a track's URL as a fixed path — cache keys hash it, and the convert-undo record restores to it. Unpinned, a dragged-in source that a conversion trashed has its URL silently follow the file into the Trash, and the undo restore becomes a move onto itself that reports success — invisible when testing with `open`, which delivers plain path URLs. Every drop is therefore pinned to `fileURLWithPath:` in `performDragOperation:`, and `didConvertTrack:` pins the undo record's source URL again in case another entry path ever leaks one through.

Dropped folders expand on a four-wide queue (`NSURLUtil`), so an unreachable mount cannot hold every later open hostage — and cannot spawn a thread per drop either. Drops go through the same `OpenRequestCoordinator` as the `AppDelegate` funnel, one coordinator for one playlist: a drop starts a new generation, so it is authoritative over an older drop *and* over a slow Open Recent, either of which is discarded if it finishes late. A folder's recursive walk filters unsupported entries before retaining and sorting them, then sorts by full path with `localizedStandardCompare:` — Finder's numeric ordering, subfolders grouped; `NSDirectoryEnumerator` hands back APFS hash order, which played an album shuffled. An explicit multi-file drop keeps its pasteboard order, which is the user's own.
