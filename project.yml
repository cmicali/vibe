name: Vibe

options:
  bundleIdPrefix: com.commonwealthrecordings
  deploymentTarget:
    macOS: "13.0"
    iOS: "26.0"
  createIntermediateGroups: true
  settingPresets: none
  developmentLanguage: en
  fileTypes:
    icon:                     # Icon Composer AppIcon.icon: treat the .icon package as a single
      file: true              # file reference (not a group to recurse into) so actool compiles it.

configs:
  Debug: debug
  Release: release

# Shared between the app targets so a second platform cannot drift from the
# first on where TagLib, PINCache and the generated git-info header live.
settingGroups:
  # THE version, for both apps. The two targets ship the same product under one
  # bundle identifier, so they release together and must carry the same number;
  # declared once here rather than once per target, which is how the iOS target
  # sat at 1.0 (1) against the mac's 1.10 (110) with nothing to catch it.
  #
  # Bumping a release means editing these two lines and nothing else.
  # release-appstore.sh and github-release.sh both read the number back out of
  # the BUILT app rather than out of this file, so a scrape cannot pick the
  # wrong target's copy.
  vibe-version:
    MARKETING_VERSION: "1.11"
    CURRENT_PROJECT_VERSION: 111

  vibe-header-paths:
    HEADER_SEARCH_PATHS:
      - "$(inherited)"
      - "$(SRCROOT)/build/generated"      # VibeGitInfo.h, written by the pre-build script
      - "$(SRCROOT)/Vibe/ThirdParty/taglib"
      - "$(SRCROOT)/Vibe/ThirdParty/taglib/toolkit"
      - "$(SRCROOT)/Vibe/ThirdParty/taglib/mpeg"
      - "$(SRCROOT)/Vibe/ThirdParty/taglib/mpeg/id3v1"
      - "$(SRCROOT)/Vibe/ThirdParty/taglib/mpeg/id3v2"
      - "$(SRCROOT)/Vibe/ThirdParty/taglib/mpeg/id3v2/frames"
      - "$(SRCROOT)/Vibe/ThirdParty/taglib/ape"
      - "$(SRCROOT)/Vibe/ThirdParty/taglib/mp4"
      - "$(SRCROOT)/Vibe/ThirdParty/taglib/flac"
      - "$(SRCROOT)/Vibe/ThirdParty/taglib/riff"
      - "$(SRCROOT)/Vibe/ThirdParty/taglib/riff/aiff"
      - "$(SRCROOT)/Vibe/ThirdParty/taglib/riff/wav"
      - "$(SRCROOT)/Vibe/ThirdParty/taglib/ogg"
      - "$(SRCROOT)/Vibe/ThirdParty"
      - "$(SRCROOT)/Vibe/ThirdParty/PINCache"
      - "$(SRCROOT)/Vibe/ThirdParty/PINOperation"

# ---- PROJECT-LEVEL settings (== PBXProject build configs) ----
settings:
  base:
    ALWAYS_SEARCH_USER_PATHS: NO
    ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS: YES
    CLANG_ANALYZER_NONNULL: YES
    CLANG_ANALYZER_NUMBER_OBJECT_CONVERSION: YES_AGGRESSIVE
    CLANG_CXX_LANGUAGE_STANDARD: "c++17"
    CLANG_CXX_LIBRARY: "libc++"
    CLANG_ENABLE_MODULES: YES
    CLANG_ENABLE_OBJC_ARC: YES
    CLANG_ENABLE_OBJC_WEAK: YES
    CLANG_WARN_BLOCK_CAPTURE_AUTORELEASING: YES
    CLANG_WARN_BOOL_CONVERSION: YES
    CLANG_WARN_COMMA: YES
    CLANG_WARN_CONSTANT_CONVERSION: YES
    CLANG_WARN_DEPRECATED_OBJC_IMPLEMENTATIONS: YES
    CLANG_WARN_DIRECT_OBJC_ISA_USAGE: YES_ERROR
    CLANG_WARN_DOCUMENTATION_COMMENTS: YES
    CLANG_WARN_EMPTY_BODY: YES
    CLANG_WARN_ENUM_CONVERSION: YES
    CLANG_WARN_INFINITE_RECURSION: YES
    CLANG_WARN_INT_CONVERSION: YES
    CLANG_WARN_NON_LITERAL_NULL_CONVERSION: YES
    CLANG_WARN_OBJC_IMPLICIT_RETAIN_SELF: YES
    CLANG_WARN_OBJC_LITERAL_CONVERSION: YES
    CLANG_WARN_OBJC_ROOT_CLASS: YES_ERROR
    CLANG_WARN_QUOTED_INCLUDE_IN_FRAMEWORK_HEADER: YES
    CLANG_WARN_RANGE_LOOP_ANALYSIS: YES
    CLANG_WARN_STRICT_PROTOTYPES: YES
    CLANG_WARN_SUSPICIOUS_MOVE: YES
    CLANG_WARN_UNGUARDED_AVAILABILITY: YES_AGGRESSIVE
    CLANG_WARN_UNREACHABLE_CODE: YES
    CLANG_WARN__DUPLICATE_METHOD_MATCH: YES
    COPY_PHASE_STRIP: NO
    DEAD_CODE_STRIPPING: YES
    DEVELOPMENT_TEAM: 4UEV752JH4
    ENABLE_STRICT_OBJC_MSGSEND: YES
    ENABLE_USER_SCRIPT_SANDBOXING: YES
    GCC_C_LANGUAGE_STANDARD: c17
    GCC_NO_COMMON_BLOCKS: YES
    # Warnings are errors everywhere: the warning set was audited to zero
    # (2026-08-21) and neither xcodebuild nor the analyze gate would otherwise
    # fail on a compiler warning — the analyze script's filter deliberately
    # matches only checker findings. ThirdParty compiles warning-free today; if
    # a future toolchain warns on vendored code the repo does not restyle, give
    # that file a per-file -Wno-* flag beside the PIN caches' entries rather
    # than turning this off.
    GCC_TREAT_WARNINGS_AS_ERRORS: YES
    GCC_WARN_64_TO_32_BIT_CONVERSION: YES
    GCC_WARN_ABOUT_RETURN_TYPE: YES_ERROR
    GCC_WARN_UNDECLARED_SELECTOR: YES
    GCC_WARN_UNINITIALIZED_AUTOS: YES_AGGRESSIVE
    GCC_WARN_UNUSED_FUNCTION: YES
    GCC_WARN_UNUSED_VARIABLE: YES
    GCC_PRECOMPILE_PREFIX_HEADER: YES
    GCC_PREFIX_HEADER: "$(SRCROOT)/Vibe/Common/Vibe-Prefix.pch"
    MACOSX_DEPLOYMENT_TARGET: "13.0"
    MTL_FAST_MATH: YES
    SDKROOT: macosx
    STRING_CATALOG_GENERATE_SYMBOLS: YES
    LOCALIZATION_PREFERS_STRING_CATALOGS: YES   # Export/Import Localizations round-trips .xcstrings
  configs:
    Debug:
      DEBUG_INFORMATION_FORMAT: dwarf
      ENABLE_TESTABILITY: YES
      GCC_DYNAMIC_NO_PIC: NO
      GCC_OPTIMIZATION_LEVEL: 0
      GCC_PREPROCESSOR_DEFINITIONS:
        - "DEBUG=1"
        - "$(inherited)"
      MTL_ENABLE_DEBUG_INFO: INCLUDE_SOURCE
      ONLY_ACTIVE_ARCH: YES
    Release:
      DEBUG_INFORMATION_FORMAT: "dwarf-with-dsym"
      ENABLE_NS_ASSERTIONS: NO
      GCC_OPTIMIZATION_LEVEL: s
      # NDEBUG is used in TagLib
      GCC_PREPROCESSOR_DEFINITIONS:
        - "NDEBUG=1"
        - "$(inherited)"
      MTL_ENABLE_DEBUG_INFO: NO

targets:
  Vibe:
    type: application
    platform: macOS
    sources:
      - path: main.m                          # repo root, outside Vibe/
      # Recursive entries, one per top-level subsystem, rather than `- path:
      # Vibe`, so adding a subsystem is a deliberate edit and per-subsystem
      # build settings have somewhere to go. Nested feature directories are
      # covered by their subsystem; a new top-level boundary needs a new entry.
      #
      # The tree is sorted by feature. THE DIRECTORY IS THE PLATFORM BOUNDARY:
      # a shared subsystem keeps its shared code at the top level and its
      # platform halves in Mac/ and iOS/ subdirectories, so neither target
      # needs a per-file — or feature-named — exclude. Every shared entry
      # carries "iOS/**" whether or not that half exists yet. See VibeiOS
      # below, the root CLAUDE.md, and make check-layout.
      - path: Vibe/Audio
        excludes: ["**/.DS_Store", "**/*.md", "iOS/**"]
      - path: Vibe/Common
        excludes: ["**/.DS_Store", "**/*.md", "iOS/**"]
      - path: Vibe/System
        excludes: ["**/.DS_Store", "**/*.md", "iOS/**"]
      - path: Vibe/Util
        excludes: ["**/.DS_Store", "**/*.md", "iOS/**"]
      - path: Vibe/WaveformUI
        excludes: ["**/.DS_Store", "**/*.md", "iOS/**"]
      - path: Vibe/Controls
        excludes: ["**/.DS_Store", "**/*.md", "iOS/**"]
      - path: Vibe/Playlist
        excludes: ["**/.DS_Store", "**/*.md", "iOS/**"]
      # Every source under here wraps its whole body in #if DEBUG, so a Release
      # build compiles them to empty objects — the channel is in both targets'
      # sources but ships in neither.
      - path: Vibe/Debug
        excludes: ["**/.DS_Store", "**/*.md", "iOS/**"]
      - path: Vibe/ThirdParty
        excludes:
          - "**/.DS_Store"
          - "**/*.md"                         # nested CLAUDE.md guidance: keep UNbundled
          - "**/*.xcprivacy"                  # PIN privacy manifests: keep UNbundled
          - "**/*.txt"                        # vendored LICENSE.txt: keep UNbundled
          - "**/LICENSE.MPL"                  # TagLib's MPL 1.1 text: same, but not a .txt
          - "**/PINDiskCache.m"               # re-added below with its per-file flags
          - "**/PINMemoryCache.m"             # same
      # The NumberObjectConversion checker fires 8 times across the two PIN
      # caches (NSNumber truthiness, PIN's own style). ThirdParty is excluded
      # from the analyze gate, but the findings still printed in every CI log;
      # vendored code is not restyled, so the checker is switched off for
      # exactly these files instead. The -Xclang pair is inert in a normal
      # compile and only takes effect under the analyzer.
      - path: Vibe/ThirdParty/PINCache/PINDiskCache.m
        compilerFlags:
          - "-fobjc-arc-exceptions"
          - "-Xclang"
          - "-analyzer-disable-checker"
          - "-Xclang"
          - "osx.NumberObjectConversion"
      - path: Vibe/ThirdParty/PINCache/PINMemoryCache.m
        compilerFlags:
          - "-Xclang"
          - "-analyzer-disable-checker"
          - "-Xclang"
          - "osx.NumberObjectConversion"
      - path: Vibe/Mac/App
        excludes:
          - "**/.DS_Store"
          - "**/*.md"
          - "**/Info.plist"                   # consumed via INFOPLIST_FILE; must NOT be bundled
      - path: Vibe/Mac/MainWindow
        excludes: ["**/.DS_Store", "**/*.md"]
      - path: Vibe/Mac/Controls
        excludes: ["**/.DS_Store", "**/*.md"]
      - path: Vibe/Mac/Menu
        excludes: ["**/.DS_Store", "**/*.md"]
      - path: Vibe/Mac/About
        excludes: ["**/.DS_Store", "**/*.md"]
      - path: Vibe/Mac/Settings
        excludes: ["**/.DS_Store", "**/*.md"]
      - path: Resources/Assets.xcassets       # .xcassets handled natively by XcodeGen
      - path: Resources/AppIcon.icon          # Icon Composer .icon (handled via options.fileTypes)
      - path: Resources/VectorBalls.metal.txt
        buildPhase: resources                 # runtime-compiled off-main; avoids optional Metal toolchain
      # String Catalogs. The build only COMPILES these; keys are extracted by
      # `make strings` (no build-time extraction exists for ObjC — see
      # scripts/extract-strings.sh, deliberately not a build phase).
      - path: Resources/Localizable.xcstrings
        buildPhase: resources
      - path: Resources/InfoPlist.xcstrings
        buildPhase: resources
      # Built-in theme names' hand-managed catalog (table "ThemeNames") — not
      # touched by extraction; covered by check-translations.
      - path: Resources/ThemeNames.xcstrings
        buildPhase: resources
      # A folder reference: every JSON dropped in Resources/Themes ships as a
      # built-in theme with no project edit — the point of the directory.
      - path: Resources/Themes
        type: folder
        buildPhase: resources
    preBuildScripts:
      # Writes build/generated/VibeGitInfo.h (commit/branch/dirty) for the
      # startup log — see NSBundle+BuildInfo. Reading .git needs
      # ENABLE_USER_SCRIPT_SANDBOXING: NO (already set below).
      - script: '"$SRCROOT/scripts/generate-git-info.sh"'
        name: Generate Git Info
        basedOnDependencyAnalysis: false
        outputFiles:
          - "$(SRCROOT)/build/generated/VibeGitInfo.h"
    info:
      path: Vibe/Mac/App/Info.plist
      properties:

        CFBundleName: "$(PRODUCT_NAME)"
        CFBundleExecutable: "$(EXECUTABLE_NAME)"
        CFBundleIdentifier: "$(PRODUCT_BUNDLE_IDENTIFIER)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        LSApplicationCategoryType: "public.app-category.music"

        NSHumanReadableCopyright: "Copyright © 2026 Christopher Micali. All rights reserved."

        ITSAppUsesNonExemptEncryption: false

        LSMultipleInstancesProhibited: YES
        NSSupportsAutomaticTermination: YES
        NSSupportsSuddenTermination: YES

        NSPrincipalClass: "NSApplication"
        LSMinimumSystemVersion: "$(MACOSX_DEPLOYMENT_TARGET)"
        CFBundleDevelopmentRegion: "$(DEVELOPMENT_LANGUAGE)"
        CFBundlePackageType: "$(PRODUCT_BUNDLE_PACKAGE_TYPE)"
        CFBundleInfoDictionaryVersion: "6.0"

        VibeBuild:
          Configuration: "$(CONFIGURATION)"
          OptimizationLevel: "-O$(GCC_OPTIMIZATION_LEVEL)"
          PreprocessorDefinitions: "$(GCC_PREPROCESSOR_DEFINITIONS)"
          OtherCFlags: "$(OTHER_CFLAGS)"
          OtherCPlusPlusFlags: "$(OTHER_CPLUSPLUSFLAGS)"
          OtherLinkerFlags: "$(OTHER_LDFLAGS)"
          LanguageStandards: "C $(GCC_C_LANGUAGE_STANDARD) / C++ $(CLANG_CXX_LANGUAGE_STANDARD) / $(CLANG_CXX_LIBRARY)"
          Architectures: "$(ARCHS)"
          DeadCodeStripping: "$(DEAD_CODE_STRIPPING)"
          HardenedRuntime: "$(ENABLE_HARDENED_RUNTIME)"
          AppSandbox: "$(ENABLE_APP_SANDBOX)"

        CFBundleDocumentTypes:
          - CFBundleTypeName: "Folder"
            CFBundleTypeRole: "Viewer"
            LSHandlerRank: "Alternate"
            LSItemContentTypes:
              - "public.folder"
          - CFBundleTypeName: "Audio Files"
            CFBundleTypeRole: "Viewer"
            LSHandlerRank: "Default"
            LSItemContentTypes:
              - "public.mp2"
              - "public.mp3"
              - "public.aac-audio"
              - "public.aiff-audio"
              - "com.microsoft.waveform-audio"
              - "org.xiph.flac"
              - "com.apple.m4a-audio"
              - "public.mpeg-4-audio"
          - CFBundleTypeName: "CUE Sheet"
            CFBundleTypeRole: "Viewer"
            LSHandlerRank: "Alternate"
            LSItemContentTypes:
              - "com.commonwealthrecordings.cue-sheet"
          - CFBundleTypeName: "M3U Playlist"
            CFBundleTypeRole: "Viewer"
            LSHandlerRank: "Alternate"
            LSItemContentTypes:
              - "public.m3u-playlist"
          - CFBundleTypeName: "Converted FLAC"
            CFBundleTypeRole: "Editor"
            LSHandlerRank: "None"
            NSIsRelatedItemType: true
            LSItemContentTypes:
              - "org.xiph.flac"

        # macOS has no system UTI for cue sheets — files come up as dyn.* — so
        UTExportedTypeDeclarations:
          - UTTypeIdentifier: "com.commonwealthrecordings.cue-sheet"
            UTTypeDescription: "CUE Sheet"
            UTTypeConformsTo:
              - "public.plain-text"
            UTTypeTagSpecification:
              public.filename-extension:
                - "cue"

    settings:
      groups: [vibe-version, vibe-header-paths]
      base:
        PRODUCT_NAME: "$(TARGET_NAME)"
        PRODUCT_BUNDLE_IDENTIFIER: com.commonwealthrecordings.Vibe
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_INCLUDE_ALL_APPICON_ASSETS: YES
        CLANG_ENABLE_MODULES: YES
        CODE_SIGN_ENTITLEMENTS: "$(SRCROOT)/Vibe/Mac/App/Vibe.entitlements"
        CODE_SIGN_IDENTITY: "-"             # Sign to Run Locally: no dev cert needed (empty
                                            # entitlements, no profile); release.sh re-signs
                                            # with Developer ID for distribution.
        CODE_SIGN_STYLE: Automatic
        COMBINE_HIDPI_IMAGES: YES
        ENABLE_APP_SANDBOX: YES
        ENABLE_HARDENED_RUNTIME: YES
        ENABLE_USER_SCRIPT_SANDBOXING: NO           # target overrides project-level YES
        ENABLE_USER_SELECTED_FILES: readwrite
        EXCLUDED_ARCHS: ""
        LD_RUNPATH_SEARCH_PATHS:
          - "$(inherited)"
          - "@executable_path/../Frameworks"
        LIBRARY_SEARCH_PATHS: "$(inherited)"
        MACOSX_DEPLOYMENT_TARGET: "13.0"
        PROVISIONING_PROFILE_SPECIFIER: ""
    dependencies:
      - sdk: AVFoundation.framework
      - sdk: Accelerate.framework
      - sdk: ApplicationServices.framework
      - sdk: AudioToolbox.framework
      - sdk: CoreAudio.framework
      - sdk: ImageIO.framework              # AudioTrackMetadata art decode, DebugUtil screenshots
      - sdk: MediaPlayer.framework
      - sdk: Metal.framework                # VectorBallsView (About window)
      - sdk: MetalKit.framework             # VectorBallsView (About window)
      - sdk: QuartzCore.framework
      - sdk: UniformTypeIdentifiers.framework  # AppDelegate file filtering, DebugUtil

  # The iOS app: the shared subset (every shared subsystem minus its Mac/
  # half, plus ThirdParty) plus the UIKit app layer in Vibe/iOS. Same bundle ID as
  # the mac app: two platforms may share one, and it is the prerequisite for
  # App Store universal purchase.
  VibeiOS:
    type: application
    platform: iOS
    sources:
      # THE DIRECTORY IS THE PLATFORM BOUNDARY. Every entry below is a whole
      # shared directory whose AppKit half lives in a Mac/ subdirectory this
      # target simply never names. So there are no per-file — or feature-named
      # — excludes here to drift, and a new file in a shared directory joins
      # this target automatically: it must be AppKit-free or
      # TARGET_OS_OSX-guarded, which CI's build-ios job enforces. See the root
      # CLAUDE.md and make check-layout.
      - path: Vibe/iOS
        excludes: ["**/.DS_Store", "**/*.md"]
      - path: Vibe/Audio
        excludes: ["**/.DS_Store", "**/*.md", "Mac/**"]
      - path: Vibe/Common
        excludes: ["**/.DS_Store", "**/*.md", "Mac/**"]
      - path: Vibe/System
        excludes: ["**/.DS_Store", "**/*.md", "Mac/**"]
      - path: Vibe/Util
        excludes: ["**/.DS_Store", "**/*.md", "Mac/**"]
      - path: Vibe/Controls
        excludes: ["**/.DS_Store", "**/*.md", "Mac/**"]
      - path: Vibe/Playlist
        excludes: ["**/.DS_Store", "**/*.md", "Mac/**"]
      - path: Vibe/WaveformUI
        excludes: ["**/.DS_Store", "**/*.md", "Mac/**"]
      # Every source under here wraps its whole body in #if DEBUG, so a Release
      # build compiles them to empty objects — the channel is in both targets'
      # sources but ships in neither.
      - path: Vibe/Debug
        excludes: ["**/.DS_Store", "**/*.md", "Mac/**"]
      - path: Vibe/ThirdParty
        excludes:
          - "**/.DS_Store"
          - "**/*.md"
          - "**/*.xcprivacy"
          - "**/*.txt"
          - "**/LICENSE.MPL"
          - "**/PINDiskCache.m"
          - "**/PINMemoryCache.m"
      # Same per-file flags as the macOS target above; see the comment there.
      - path: Vibe/ThirdParty/PINCache/PINDiskCache.m
        compilerFlags:
          - "-fobjc-arc-exceptions"
          - "-Xclang"
          - "-analyzer-disable-checker"
          - "-Xclang"
          - "osx.NumberObjectConversion"
      - path: Vibe/ThirdParty/PINCache/PINMemoryCache.m
        compilerFlags:
          - "-Xclang"
          - "-analyzer-disable-checker"
          - "-Xclang"
          - "osx.NumberObjectConversion"
      - path: Resources/AppIcon.icon
      - path: Resources/Assets.xcassets       # record-bg, the no-art vinyl placeholder
      - path: Resources/PrivacyInfo.xcprivacy
        buildPhase: resources
      - path: Resources/Localizable.xcstrings
        buildPhase: resources
      - path: Resources/InfoPlist.xcstrings
        buildPhase: resources
    preBuildScripts:
      - script: '"$SRCROOT/scripts/generate-git-info.sh"'
        name: Generate Git Info
        basedOnDependencyAnalysis: false
        outputFiles:
          - "$(SRCROOT)/build/generated/VibeGitInfo.h"
    info:
      path: Vibe/iOS/Info.plist
      properties:
        CFBundleName: "$(PRODUCT_NAME)"
        CFBundleExecutable: "$(EXECUTABLE_NAME)"
        CFBundleIdentifier: "$(PRODUCT_BUNDLE_IDENTIFIER)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleDevelopmentRegion: "$(DEVELOPMENT_LANGUAGE)"
        CFBundlePackageType: "$(PRODUCT_BUNDLE_PACKAGE_TYPE)"
        CFBundleInfoDictionaryVersion: "6.0"
        LSRequiresIPhoneOS: true
        LSApplicationCategoryType: "public.app-category.music"
        ITSAppUsesNonExemptEncryption: false
        NSHumanReadableCopyright: "Copyright © 2026 Christopher Micali. All rights reserved."
        UILaunchScreen: {}                    # system launch screen; no storyboard
        UIBackgroundModes: [audio]
        UISupportedInterfaceOrientations: [UIInterfaceOrientationPortrait, UIInterfaceOrientationLandscapeLeft, UIInterfaceOrientationLandscapeRight]
        # All four on iPad: required for a non-fullscreen app, and iPadOS 26
        # windowing lets the user give the window any aspect.
        UISupportedInterfaceOrientations~ipad: [UIInterfaceOrientationPortrait, UIInterfaceOrientationPortraitUpsideDown, UIInterfaceOrientationLandscapeLeft, UIInterfaceOrientationLandscapeRight]
        UIApplicationSceneManifest:
          # One scene, deliberately: PlayerViewController owns the audio
          # engine per-instance, so a second scene would spawn a second
          # engine. The single scene is still freely resizable on iPadOS 26.
          UIApplicationSupportsMultipleScenes: false
          UISceneConfigurations:
            UIWindowSceneSessionRoleApplication:
              - UISceneConfigurationName: Default
                UISceneDelegateClassName: VibeiOSSceneDelegate
        # Files-app integration: open-in-place from Files/share sheet, and a
        # visible on-device Documents folder (also the simulator test loop).
        LSSupportsOpeningDocumentsInPlace: true
        UIFileSharingEnabled: true
        # Same key and UTIs as the mac target's "Audio Files" entry, so
        # InfoPlist.xcstrings localizes both. No Folder claim (Launch Services
        # folder handling is a mac concept) and no related-item FLAC entry
        # (Convert is not compiled on iOS).
        CFBundleDocumentTypes:
          - CFBundleTypeName: "Audio Files"
            CFBundleTypeRole: "Viewer"
            LSHandlerRank: "Default"
            LSItemContentTypes:
              - "public.mp2"
              - "public.mp3"
              - "public.aac-audio"
              - "public.aiff-audio"
              - "com.microsoft.waveform-audio"
              - "org.xiph.flac"
              - "com.apple.m4a-audio"
              - "public.mpeg-4-audio"
        VibeBuild:
          Configuration: "$(CONFIGURATION)"
          OptimizationLevel: "-O$(GCC_OPTIMIZATION_LEVEL)"
          PreprocessorDefinitions: "$(GCC_PREPROCESSOR_DEFINITIONS)"
          OtherCFlags: "$(OTHER_CFLAGS)"
          OtherCPlusPlusFlags: "$(OTHER_CPLUSPLUSFLAGS)"
          OtherLinkerFlags: "$(OTHER_LDFLAGS)"
          LanguageStandards: "C $(GCC_C_LANGUAGE_STANDARD) / C++ $(CLANG_CXX_LANGUAGE_STANDARD) / $(CLANG_CXX_LIBRARY)"
          Architectures: "$(ARCHS)"
          DeadCodeStripping: "$(DEAD_CODE_STRIPPING)"
          HardenedRuntime: "$(ENABLE_HARDENED_RUNTIME)"
          AppSandbox: "$(ENABLE_APP_SANDBOX)"
    settings:
      groups: [vibe-version, vibe-header-paths]
      base:
        PRODUCT_NAME: Vibe
        PRODUCT_BUNDLE_IDENTIFIER: com.commonwealthrecordings.Vibe
        SDKROOT: iphoneos
        SUPPORTED_PLATFORMS: "iphoneos iphonesimulator"  # settingPresets: none injects neither
        IPHONEOS_DEPLOYMENT_TARGET: "26.0"
        TARGETED_DEVICE_FAMILY: "1,2"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_INCLUDE_ALL_APPICON_ASSETS: YES
        CODE_SIGN_STYLE: Automatic
        ENABLE_USER_SCRIPT_SANDBOXING: NO     # generate-git-info.sh reads .git
        PROVISIONING_PROFILE_SPECIFIER: ""
        LD_RUNPATH_SEARCH_PATHS:
          - "$(inherited)"
          - "@executable_path/Frameworks"
    dependencies:
      - sdk: AVFoundation.framework
      - sdk: AVKit.framework                # AVRoutePickerView (OutputRouteView)
      - sdk: Accelerate.framework
      - sdk: AudioToolbox.framework
      - sdk: ImageIO.framework
      - sdk: MediaPlayer.framework
      - sdk: QuartzCore.framework
      - sdk: UniformTypeIdentifiers.framework

  # Not a test suite: the interactive touch driver for agentic workflows —
  # the iOS counterpart of the debug channel's key/click injection, which
  # UIKit cannot do in-process (no public UITouch synthesis). One resident
  # XCUITest command loop; drive-ios.sh in the vibe-debug skill starts it and
  # sends gestures. Sources live under Tests/ for the same reason VibeTests'
  # do: the app targets recurse whole subsystem directories under Vibe/.
  VibeiOSDriver:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - path: Tests/iOSDriver
    dependencies:
      - target: VibeiOS
    settings:
      base:
        PRODUCT_NAME: "$(TARGET_NAME)"
        PRODUCT_BUNDLE_IDENTIFIER: com.commonwealthrecordings.VibeiOSDriver
        TEST_TARGET_NAME: VibeiOS
        SDKROOT: iphoneos
        SUPPORTED_PLATFORMS: "iphoneos iphonesimulator"
        IPHONEOS_DEPLOYMENT_TARGET: "26.0"
        TARGETED_DEVICE_FAMILY: "1,2"       # the driver must install beside the app on iPad sims too
        CODE_SIGN_STYLE: Automatic
        GCC_PREFIX_HEADER: ""               # no app pch; XCTest only

  # Host-LESS logic tests: no TEST_HOST, so the suite never launches the app
  # (which would boot the window, engine and caches on every run) and never
  # inherits its sandbox (which would block reading fixtures from the repo).
  # The tradeoff is that sources under test are compiled in rather than linked
  # from the app — hence the curated list below, which is the point: the
  # tested surface is a deliberate choice, not whatever the glob swept up.
  #
  # Tests live in /Tests, OUTSIDE Vibe/, because the app targets list whole
  # subsystem directories and XcodeGen recurses each one — a test file dropped
  # into any of them would be compiled into the shipping binaries.
  VibeTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      # The bundled built-in themes, so the suite's loader reads the SAME
      # files the app ships and testBundledThemesAreValid gates a theme PR.
      - path: Resources/Themes
        type: folder
        buildPhase: resources
      - path: Tests
        excludes:
          - "**/.DS_Store"
          - "**/*.md"
          - "iOSDriver/**"                    # VibeiOSDriver's XCUI sources: iOS-only API
      - path: Vibe/Audio/Analysis/AudioBPMAnalyzer.mm
      - path: Vibe/Audio/Analysis/AudioKeyAnalyzer.mm
      - path: Vibe/Audio/Waveform/AudioWaveform.mm
      - path: Vibe/Audio/Waveform/AVFAudioWaveformLoader.mm
      - path: Vibe/Audio/Waveform/AudioWaveformLoader.mm
      - path: Vibe/Debug/AudioLoadTiming.m
      - path: Vibe/Debug/AudioLoadingConfiguration+Debug.m
      - path: Vibe/Debug/DebugWireFormat.m
      - path: Vibe/Debug/VibeFakeCloud.m
      - path: Vibe/Audio/AudioTrack.m
      - path: Vibe/Audio/AudioFileFormat.m
      - path: Vibe/Audio/AudioError.m
      - path: Vibe/Audio/AudioLoadingConfiguration.m
      - path: Vibe/Audio/AudioFileMaterializationCoordinator.m
      - path: Vibe/Audio/AudioWorkScheduler.m
      - path: Vibe/Audio/Levels/AudioLevelAnalyzer.m
      - path: Vibe/Audio/Levels/AudioLevelPublisher.m
      - path: Vibe/Audio/Metadata/AudioTrackMetadataLoader.m
      - path: Vibe/Audio/Metadata/MetadataParseCoordinator.m
      - path: Vibe/Audio/PlaybackRequestCoordinator.m
      - path: Vibe/Audio/Metadata/FolderArtResolver.m
      - path: Vibe/Audio/Metadata/FolderArtEntry.m
      - path: Vibe/Audio/Metadata/FolderArtFileIO.m
      - path: Vibe/Audio/Metadata/AudioTrackArtwork.m
      - path: Vibe/Audio/Metadata/ArtworkLoadRegistry.m
      - path: Vibe/Audio/CloudTransferRegistry.m
      - path: Vibe/Common/AppSettings.m
      - path: Vibe/Common/Mac/AppSettings+Mac.m
      - path: Vibe/Common/Mac/AppTheme.m
      - path: Vibe/Common/Mac/AppTheme+Archive.m
      - path: Vibe/System/CloudFileMaterializer.m
      - path: Vibe/System/DownloadProgressMonitor.m
      - path: Vibe/System/DownloadProgressSourceAdapters.m
      - path: Vibe/Mac/App/FolderAccessManager.m
      - path: Vibe/Mac/App/OpenBurstCoalescer.m
      - path: Vibe/Mac/App/OpenRequestCoordinator.m
      - path: Vibe/Playlist/Playlist.m
      - path: Vibe/Util/NSURLUtil.m
      - path: Vibe/Util/UIUpdateTimer.m
      - path: Vibe/Playlist/PlaylistFile.m
      - path: Vibe/Common/PlayableExtensions.m
      - path: Vibe/Mac/About/VibeLinkLabel.m
      - path: Vibe/Util/Formatters.m
      - path: Vibe/Util/Categories/NSURL+Hash.m
      - path: Vibe/Util/Categories/NSURL+AudioOpen.m
      - path: Vibe/Util/Categories/NSURL+FileIdentity.m
      - path: Vibe/iOS/PageWaveformCoordinator.m
      - path: Vibe/iOS/FileSearchIndex.m
      - path: Vibe/Common/PlatformImage.m
      - path: Vibe/Common/PlatformColor.m
      - path: Vibe/Util/Mac/NSImage+Util.m
      - path: Vibe/WaveformUI/WaveformTheme.m
    settings:
      base:
        PRODUCT_NAME: "$(TARGET_NAME)"
        PRODUCT_BUNDLE_IDENTIFIER: com.commonwealthrecordings.VibeTests
        GENERATE_INFOPLIST_FILE: YES
        CODE_SIGN_IDENTITY: "-"
        CODE_SIGN_STYLE: Automatic
        PROVISIONING_PROFILE_SPECIFIER: ""
        # Deliberately NOT set: ENABLE_APP_SANDBOX, ENABLE_HARDENED_RUNTIME
        FRAMEWORK_SEARCH_PATHS:
          - "$(inherited)"
          - "$(PLATFORM_DIR)/Developer/Library/Frameworks"
        OTHER_LDFLAGS:
          - "$(inherited)"
          - "-framework"
          - "XCTest"
        LD_RUNPATH_SEARCH_PATHS:
          - "$(inherited)"
          - "@loader_path/../Frameworks"
          - "$(PLATFORM_DIR)/Developer/Library/Frameworks"
        HEADER_SEARCH_PATHS:
          - "$(inherited)"
          - "$(SRCROOT)/Vibe/Common"
          - "$(SRCROOT)/Vibe/System"
          - "$(SRCROOT)/Vibe/Mac/App"
          - "$(SRCROOT)/Vibe/Playlist"
          - "$(SRCROOT)/Vibe/Util"
          - "$(SRCROOT)/Vibe/Util/Categories"
          - "$(SRCROOT)/Vibe/Util/Mac"
          - "$(SRCROOT)/Vibe/Audio"
          - "$(SRCROOT)/Vibe/Audio/Levels"
          - "$(SRCROOT)/Vibe/Audio/Metadata"
          - "$(SRCROOT)/Vibe/Audio/Waveform"
          - "$(SRCROOT)/Vibe/Audio/Analysis"
          # Header-only activity/animation seams shared by both controls.
          - "$(SRCROOT)/Vibe/Controls"
          # WaveformZoomMath.h only: header-only, so nothing from the rendering
          # half is compiled in here.
          - "$(SRCROOT)/Vibe/WaveformUI"
          - "$(SRCROOT)/Vibe/Debug"
          - "$(SRCROOT)/Vibe/Mac/MainWindow"
          - "$(SRCROOT)/Vibe/Audio/Mac/Convert"
          - "$(SRCROOT)/Vibe/Mac/Settings"
          # PINCache's angle-bracket self-import resolves from the vendored root.
          - "$(SRCROOT)/Vibe/ThirdParty"
          # PlayerScreenRules.h only: header-only and Foundation-only, so the
          # iOS screen's state machine is testable from this macOS suite. No
          # source under Vibe/iOS is compiled here.
          - "$(SRCROOT)/Vibe/iOS"
    dependencies:
      - sdk: Accelerate.framework            # vDSP, throughout AudioWaveform
      - sdk: AppKit.framework                # AudioTrack album art (NSImage)
      - sdk: AVFoundation.framework          # AVFAudioWaveformLoader reads (and its tests write) files

schemes:
  VibeiOS:
    build:
      parallelizeBuild: true
      targets:
        VibeiOS: all
    run:      { config: Debug }
    profile:  { config: Release }
    analyze:  { config: Debug }
    archive:  { config: Release }
  # The touch driver: `xcodebuild test -scheme VibeiOSDriver` is how
  # drive-ios.sh starts the resident command loop.
  VibeiOSDriver:
    build:
      parallelizeBuild: true
      targets:
        VibeiOSDriver: [test]
    test:
      config: Debug
      targets:
        - VibeiOSDriver
  Vibe:
    build:
      parallelizeBuild: true
      targets:
        Vibe: [run, profile, analyze, archive]
    run:      { config: Debug }
    test:
      config: Debug
      targets:
        - VibeTests
    profile:  { config: Release }
    analyze:  { config: Debug }
    archive:  { config: Release }
