---
description: Full fresh audit of the codebase — bugs, performance, quality, organization
---

If `$ARGUMENTS` is non-empty, use it as the focus area. Otherwise, ask the user what to focus on using the AskUserQuestion tool with the options "Whole codebase" and "Working Changes" (the user can pick "Other" to fill in their own).

Then:

Perform a full fresh audit of the codebase, look for bugs, performance issues, code quality, and organization. Focus on the chosen focus area. Present the results in discrete lettered sections (A, B, C) with individual numbered issues (1, 2, 3) for easy cherry picking on which issues to tackle.
